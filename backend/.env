# .env

# SECURITY WARNING: keep this secret in production!
# Generate a new one with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=@w+k9ipfzh&z7kg+9q3)lo9&)m)#kdf!*@$5%n*#8qz+op-7dv

# Debug
DEBUG=True

# Hosts
ALLOWED_HOSTS=127.0.0.1,localhost

# Database
# Using SQLite for local development (no PostgreSQL needed locally)
DATABASE_URL=sqlite:///db.sqlite3

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# CORS
CORS_ALLOW_ALL_ORIGINS=True

# Email (console = prints emails to terminal, no real email needed)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
```